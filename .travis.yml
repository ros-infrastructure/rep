language: python
python:
  - "2.7"
  - "3.6"
# command to install dependencies
install:
  - pip install docutils
  - pip install xmlschema
# command to run tests
script:
  - make
notifications:
  email: false

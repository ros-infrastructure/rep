language: python
jobs:
  include:
  - python: "2.7"
    install:
    - pip install docutils
    - pip install xmlschema<1.1
  - python: "3.6"
    install:
    - pip install docutils
    - pip install xmlschema
# command to run tests
script:
  - make
notifications:
  email: false
